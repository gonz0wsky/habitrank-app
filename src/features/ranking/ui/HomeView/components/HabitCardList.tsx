import { Habit } from "@features/ranking/domain/models/Habit";
import { FC } from "react";

type Props = {
  habits: Habit[];
};

const HabitCardList: FC<Props> = () => {
  return <></>;
};

export { HabitCardList, Props as HabitCardListProps };
